# Medster Environment Configuration

# ====== LLM Provider Configuration ======
# Choose provider: "claude" or "openai"
LLM_PROVIDER=openai
LLM_MODEL=gpt-5.1

# OpenAI API Key (required for GPT-5.1, GPT-5, GPT-4o)
OPENAI_API_KEY=your_openai_api_key_here

# GPT-5.1 Reasoning Effort (default: none for low latency)
# Options: none (fastest), low, medium, high (most thorough)
REASONING_EFFORT=none

# Anthropic API Key (required for Claude Sonnet 4.5)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ====== Data Configuration ======
# Coherent Data Set Configuration
# Download 9GB dataset from: https://synthea.mitre.org/downloads
# Contains FHIR, DICOM, genomics, ECGs, and clinical notes all linked together
# Extract and set path to the FHIR folder
COHERENT_DATA_PATH=./coherent_data/fhir

# Multimodal data paths for vision analysis
COHERENT_DICOM_PATH=./coherent_data/dicom      # Brain MRI scans
COHERENT_DNA_PATH=./coherent_data/dna          # Genomic variant data
COHERENT_CSV_PATH=./coherent_data/csv          # Observations with ECG images

# ====== MCP Server Configuration ======
# MCP Medical Analysis Server (your FastMCP server)
# For complex note analysis
MCP_SERVER_URL=http://localhost:8000
MCP_DEBUG=false

# ====== Reasoning Mode Configuration ======
# Toggle between reasoning modes:
#   - "deterministic" = Original Medster prompts (fast, simple)
#   - "bayesian" = Probabilistic reasoning with confidence tracking
REASONING_MODE=deterministic
